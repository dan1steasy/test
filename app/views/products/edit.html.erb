<% @page_title = "Edit product" -%>
<% @topnav_info = {:object_id => @product.id.to_s} -%>

<!--[div:contentleft]-->
<div id="contentleft">
  <h2>Edit product details</h2>
  <p>Use the form below to edit the details of this product.</p>
  <%= start_form_tag :action => 'update', :id => @product %>
  <table>
    <tr>
      <td><label for="product_category_id">Category</label></td>
      <td><%= collection_select :product, :category_id, @categories, :id, :name %></td>
    </tr>
    <%= render :partial => 'form' %>
    <tr>
      <td><%= submit_tag "Update product" %></td>
    </tr>
  </table>
  <%= end_form_tag %>
</div>
<!--[eodiv:contentleft]-->
